[tool.poetry]
name = "jupyter"
version = "0.1.0"
description = ""

[tool.poetry.dependencies]
python = "^3.9"
jupyterlab = "^3.3.3"
hatchling = "^0.22.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
